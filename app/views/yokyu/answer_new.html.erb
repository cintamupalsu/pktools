<% provide(:title, "新規列所子") %>
<div class="row">
  <aside class="col-xl-1">
    <%= render 'sidemenu' %>
  </aside>
  <div class="col-xl-11">
    <div class="card" id="question">
      <div class="card-header text-center">
        <strong>新規回答 </strong>(<%= link_to @question.name, question_path(:id => @question.id) %>)
      </div>
      <div class="card-body">
        <%= form_for(:answer, url: answer_new_path) do |f| %>
     
          <%= f.label :name, "名前" %>
          <%= f.text_field :name, placeholder: "回答" , class: 'form-control'  %>
          </br>
          <%= f.label :column, "列所" %>
          <%= f.text_field :column,  placeholder: "A" , class: 'form-control'  %>
          
          <%= f.hidden_field :question_id, value: @question.id %>
          </br>
            <%= f.submit '列所登録', class: "btn btn-outline-success fluid" %>
        <% end %>
      </div>
    </div>
  </div>
</div>
